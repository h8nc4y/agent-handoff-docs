# START_HERE — <project>

> 全リポジトリ共通の「引き継ぎの入口」ファイル。新しく参加するエージェント
> （または人間）が最初に読み、正本の所在と着手手順を把握する。時点情報を
> 書かないことで、このファイル自体が陳腐化しないように保つ。

## この資料の正本責務

このファイルが単一の正本として持つもの: このリポジトリの読み順と入口の
案内。加えて、下の検証コマンドと主要ゲートは「他に所有する資料がない
場合のみ」ここが正本（`AGENTS.md` のような規約資料が所有する場合、該当
節はそこへのポインタにする。所有権は移動するのであって共有しない）。
週単位で変わる情報は一切持たない — 現在地は `HANDOFF.md`、作業キューは
`TASKS.md` が正本。本ファイルと実状態が食い違ったら、実状態（リポジトリ
の観測結果）が正であり、直すのはこのファイルの側。

## このリポジトリは何か

<1〜2行。目的だけを書く。週単位で変わることは書かない。>

## 読み順（正本）

1. `README.md` — 概要・公開導線
2. `docs/REQUIREMENTS.md` — 要件と受け入れ基準（意図の正本）
3. `docs/ARCHITECTURE.md` — 現行の as-built 設計
4. `HANDOFF.md` — 現在地
5. `TASKS.md` — タスク台帳

<ファイル名・パスはリポジトリに合わせて調整してよいが、
「概要 → 意図 → 設計 → 現在地 → 作業キュー」の順序は保つ。
経緯（`docs/REPORT.md`）は必要時のみ読む。>

## 検証コマンド

PR 前に必ず通すもの:

```
<build / test / lint / scan コマンドをそのまま書く。
または、それらを正本として持つ資料の節を1つだけ指す>
```

## 主要ゲート（承認なしに越えない境界）

- <例: 本番デプロイ禁止 / 外部発信禁止 / 有料 API 使用禁止>
- <例: 保護ブランチ、オーナー承認が要る操作>

<規約資料がゲートを所有する場合は、この一覧をそこへのポインタに
置き換える。>

## 次の一手

`HANDOFF.md` の「次の一手」節を読むこと。本ファイルには時点情報を
意図的に置いていない — ここに現在地を書き足さないこと。
