# REQUIREMENTS（要件定義書）— <project>

最終更新: <date>（<author/agent>）

## この資料の正本責務

このファイルが単一の正本として持つもの: <project> が満たすべきこと
（機能要件・非機能要件）、やらないこと（非スコープ）、および受け入れ
基準とその現在ステータス。下の受け入れ基準表が、要件レベルの「残作業の
正本」になる。持たないもの: 現行の実装構造（`docs/ARCHITECTURE.md`）、
経緯・実測記録（`docs/REPORT.md`）、運用上の作業キュー（`TASKS.md`）、
現在地（`HANDOFF.md`）。本資料と実状態が食い違ったら実状態が正。

## 目的・背景

<このプロジェクトが存在する理由を短く。検討過程や不採用案は
`docs/REPORT.md` に置く。>

## 機能要件

| ID | 要件 | 優先度 | 備考 |
| --- | --- | --- | --- |
| FR-1 | <システムは〜すること> | must | |
| FR-2 | <システムは〜すること> | should | |

## 非機能要件

| ID | 要件 | 備考 |
| --- | --- | --- |
| NFR-1 | <性能 / セキュリティ / 可搬性の制約> | |

## 非スコープ（やらないこと）

- <次のエージェントが誤って作らないよう、明示的にスコープ外を書く>

## 受け入れ基準

ステータス規則: `done` にできるのは、検証列のコマンド・手順が生んだ
証跡（コマンド出力・CI 実行・PR 参照）があるときだけ。日付や数値は
記憶からではなくリポジトリから読み直して書く。それ以外はすべて
`not yet` のままにする。`not yet` の行が残作業の正本。

| ID | 基準（観測可能な結果） | 検証方法（コマンド / 手順） | 状態 | 証跡 |
| --- | --- | --- | --- | --- |
| AC-1 | <満たされたとき何が観測できるか> | <確認方法> | not yet | |
| AC-2 | <満たされたとき何が観測できるか> | <確認方法> | not yet | |

## 未検証チェックリスト

各項目に「何を実行すれば検証できるか」を書く。このリストの消し込みが、
そのまま次のエージェントへの作業指示になる。

- [ ] <主張> — 検証: `<command>`

## 未決事項

| ID | 論点 | 判断者 | 状態 |
| --- | --- | --- | --- |
| Q-1 | <人間/オーナーの判断が要ること> | <誰> | open |
