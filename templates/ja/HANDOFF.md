# HANDOFF（引き継ぎ）— <project>

最終更新: <date>（<author/agent>）

## この資料の正本責務

このファイルが単一の正本として持つもの: 現在地と次の一手 — それだけ。
持たないもの: 要件（`docs/REQUIREMENTS.md`）、現行設計
（`docs/ARCHITECTURE.md`）、経緯・実測記録（`docs/REPORT.md`）、
作業キュー（`TASKS.md`）、規約・ゲート（<このリポジトリで所有している
側: 規約資料（例 `AGENTS.md`）または `START_HERE.md`>）。
本資料と実状態が食い違ったら実状態が正 — まず `git log` と open PR を
確認し、直すのはこのファイルの側。

<注: `docs/ARCHITECTURE.md` と `docs/REPORT.md` の雛形は同梱されて
いない — この参照は、このリポジトリでその役割を担うものに合わせて
調整する（小規模なら README の1節と git 履歴で代替可）。>

サイズ予算: 目安 1,000 トークン以下に保つ。作業の区切りごと・最終報告の
前に必ず圧縮する: 残す価値のある経緯は `docs/REPORT.md` へ移してから、
ここから削る。secret・token・credential 入りログ・実データは絶対に
置かない（このファイルは今後の全セッションが読む場所だから）。

## 触る前に知るべき罠

- <知らないと次のエージェントが1時間を失う事実を1〜2点>

## 現在のゴールと成功指標

- ゴール: <この作業の目的>
- 完了条件: <観測可能な成功条件>

## 現在地

- <3〜8個の箇条書き。現在形で。経緯は書かない>

## Key files

- `<path>` — <いまなぜ重要か>

## 直近の決定

- <決定を1行で。背景・不採用案は docs/REPORT.md へ>

## 実行済みコマンド

- `<command>` — <結果を数語で>

## 既知の問題

- <問題と、どこで追跡しているか（TASKS の ID）>

## Do not re-read（再読不要）

- `<旧資料やパス>` — <commit / PR 参照> で削除・置き換え済み。残す価値の
  ある内容は `<正本資料>` へ反映済み。git 履歴から復元可能。

## 次の一手

1. <質問なしで着手できる、最小の次アクション>
